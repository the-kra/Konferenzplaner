# ============================================================
#  Whitelist-Prinzip: ALLES ignorieren ...
# ============================================================
*

# ... AUSSER den folgenden Dateien/Ordnern:
!.gitignore
!README.md
!DEPLOYMENT.md
!index.html

# Ordner müssen erst „durchlässig" gemacht werden, damit die
# Whitelist-Regeln darin greifen:
!tools/
tools/*
!tools/untis-export.js
# anonymisierte BEISPIEL-Daten (keine echten Personen):
!tools/beispiel_klassen_lehrer.csv

# ------------------------------------------------------------
# Bewusst NICHT im Repo (lokale Daten / Müll):
#   *.csv  *.xlsx  *.xls   -> echte Lehrer-/Klassendaten (Datenschutz!)
#   .DS_Store              -> macOS-Müll
#   node_modules, *.log    -> falls je vorhanden
# ------------------------------------------------------------
